# Production Environment Configuration for Shopify Lifetime Value Discounts
# Copy this file to .env on your production server and fill in the values

# Shopify App Configuration
SHOPIFY_API_KEY=your_shopify_api_key
SHOPIFY_API_SECRET=your_shopify_api_secret_production

# Production App URL (your hosting provider URL)
# Examples: 
# - Heroku: https://your-app-name.herokuapp.com
# - Fly.io: https://your-app-name.fly.dev
# - Custom domain: https://app.yourdomain.com
SHOPIFY_APP_URL=https://your-production-url.com

# Required Shopify API scopes (must match shopify.app.toml)
SCOPES=read_customers,write_customers,read_orders,write_discounts,read_discounts,write_price_rules,read_price_rules

# Database Configuration
# Use a production database URL (PostgreSQL recommended)
# Examples:
# - PostgreSQL: postgresql://user:password@host:5432/dbname
# - MySQL: mysql://user:password@host:3306/dbname
DATABASE_URL=postgresql://user:password@host:5432/shopify_ltv_discounts

# Server Configuration
PORT=8080
HOST=0.0.0.0

# Session Storage (generate a strong secret for production)
# Generate with: openssl rand -base64 32
SESSION_SECRET=generate_a_strong_random_32_character_string_for_production

# Optional: Custom domain if different from SHOPIFY_APP_URL
# SHOP_CUSTOM_DOMAIN=your-custom-domain.com

# Environment Configuration
NODE_ENV=production
APP_ENV=production

# Optional: Error tracking and monitoring
# SENTRY_DSN=your_sentry_dsn_here
# NEW_RELIC_LICENSE_KEY=your_new_relic_key_here

# Optional: Redis for session storage (recommended for production)
# REDIS_URL=redis://user:password@host:6379/0

# Optional: CDN configuration
# CDN_URL=https://cdn.yourdomain.com

# Security Headers (optional but recommended)
# FORCE_SSL=true
# SECURE_COOKIES=true